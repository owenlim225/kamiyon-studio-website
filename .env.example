# Sanity CMS (required for Studio and live CMS fetches)
CMS_PROJECT_ID=
CMS_DATASET=production

# Optional: read token for private datasets or server-side fetches
CMS_API_TOKEN=

# Site URL for sitemap, canonical URLs, and Open Graph (production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
