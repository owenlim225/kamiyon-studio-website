# Local Wrangler / OpenNext preview secrets (gitignored).
# Copy to `.dev.vars` and fill values. Never commit real secrets.
# Mirror the same names used in `.env.local` for Next API routes.

APP_ENV=local
# MEDIA_UPLOAD_SECRET=
# SANITY_REVALIDATE_SECRET=
# SANITY_API_READ_TOKEN=
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# NEXT_PUBLIC_SANITY_PROJECT_ID=
# NEXT_PUBLIC_SANITY_DATASET=production
# NEXT_PUBLIC_R2_PUBLIC_BASE_URL=
